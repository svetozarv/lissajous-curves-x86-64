#include <stdio.h>
#include <stdlib.h>
#include <SDL2/SDL.h>
#include "lissajous.h"

    

int main(int argc, char* argv[]) {
    // 1. Allocate memory buffer for the image
    // 2. Fill with 0 and display to user
    // in loop:
    // 3. Get user input
    // 4. Call an assembly func
    // 5. Refresh the screen

    // allocateBuffers();
    // readDataFromFiles(); // if required
    // setParamsToDefaultValues();
    // while(true) {
	    // f(...); // call assembly function
	    // displayResult();
	    // readUserInput(); // this function shall block and wait for user interaction
	    // modifyParams(); // according to user input
    // }


    return 0;
}
